const UpVotes = require('../../api/v1/upVotes/model');
const { BadRequestError, NotFoundError } = require('../../errors');

const createUpVotes = async (req, res) => {
  const { id } = req.params;
};
